=== Case Study : Scene Builder

The SceneBuilder application is a Java application which can be used to create FXML definition files and JavaFX CSS style files. 

It is an example of a non trivial application.